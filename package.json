{
  "name": "causal-market-regime-detection-root",
  "version": "1.0.0",
  "scripts": {
    "vercel-build": "cd web-ui && npm install && npm run build && cd .. && node -e \"const fs = require('fs'); fs.rmSync('public', {recursive:true, force:true}); fs.renameSync('web-ui/dist', 'public');\"",
    "install": "echo 'No root dependencies'",
    "postinstall": "cd web-ui && npm install"
  }
}
